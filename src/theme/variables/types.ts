export interface TColors {
  blue: string;
  blueDarker: string;
  gray: string;
  orange: string;
  red: string;
  redDarker: string;
}

export interface TFonts {
  bold: number;
  initialFontSize: number;
  medium: number;
  normal: number;
}

export interface TDevices {
  mobile: string;
  tablet: string;
}
